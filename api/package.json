{
  "name": "lpg-info-api",
  "version": "1.0.0",
  "description": "LPG Consumer Info API via UMANG",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
